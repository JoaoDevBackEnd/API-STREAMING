class Director  {
    constructor(id, name, profile,createdAt,birthDate) {
      this.id = id;
      this.name = name;
      this.profile = profile;
      this.createdAt=createdAt;
      this.birthDate=birthDate;
    }
  }
  
  export default Director;