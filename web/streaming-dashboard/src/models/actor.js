class Actor  {
    constructor(id, name, profile,createdAt) {
      this.id = id;
      this.name = name;
      this.profile = profile;
      this.profile=createdAt;
    }
  }
  
  export default Actor;
  